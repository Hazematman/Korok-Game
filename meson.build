project('korok_game', 'cpp')

sdl2 = dependency('SDL2')
opengl = dependency('OpenGL')
bullet = dependency('bullet')

srcs = ['src/main.cpp']
executable('korok_game', srcs, dependencies : [sdl2, opengl, bullet])
